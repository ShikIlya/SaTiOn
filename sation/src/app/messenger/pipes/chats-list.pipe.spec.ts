import { ChatsListPipe } from './chats-list.pipe';

describe('ChatsListPipe', () => {
  it('create an instance', () => {
    const pipe = new ChatsListPipe();
    expect(pipe).toBeTruthy();
  });
});
